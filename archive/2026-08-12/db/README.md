# db/ — сюда кладётся дамп базы

Папка НАМЕРЕННО пуста на момент создания архива. Дамп снимается через `/api/backup?mode=dump`
после того, как владелец задаст `BACKUP_TOKEN` в окружении прода — см. `../MANIFEST.md`.

Ожидаемое содержимое:
  counts-prod.json          сверка строк по каждой таблице, снятая С ПРОДА
  edge-2026-08-12.db        согласованный снимок SQLite (VACUUM INTO)
  counts-restored.json      те же счётчики, снятые С ФАЙЛА ДАМПА — сверка полноты

Пустая папка здесь честнее отсутствующей: она называет, чего в архиве не хватает.
